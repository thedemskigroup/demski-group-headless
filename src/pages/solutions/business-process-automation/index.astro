---
import Layout from '../../../layouts/Layout.astro';
import ContactSection from '../../../components/ContactSection.astro';

const assetBase = '/solutions/sales-lead-tracking-tools';

const strategicBenefits = [
  ['fa-table-cells-large', 'Eliminate Manual Bottlenecks', 'Replace slow, error-prone manual steps with automated workflows that move tasks through your organization instantly and accurately.'],
  ['fa-square-check', 'Reduce Operational Costs', 'Automation reduces the overhead of repetitive back-office work — cutting labor costs, minimizing rework, and freeing your team for higher-value tasks.'],
  ['fa-layer-group', 'Improve Process Consistency', 'Standardized automated processes ensure every task follows the same rules every time — no missed steps, no human variation, no compliance gaps.'],
  ['fa-laptop-code', 'Gain Real-Time Visibility', 'Track the status of every process, task, and approval in real time with dashboards and automated reporting built into your workflows.'],
  ['fa-shield-halved', 'Scale Without Growing Headcount', 'As your business grows, your automated processes scale with you — handling higher volumes without requiring proportional increases in staff.']
];

const services = [
  ['fa-sitemap', 'Process Discovery & Mapping', 'We audit your current workflows end-to-end, identifying inefficiencies, redundancies, and high-impact automation opportunities before writing a single line of code.'],
  ['fa-robot', 'Robotic Process Automation (RPA)', 'Deploy software bots that handle repetitive, rules-based tasks — data entry, document processing, system updates — with speed and precision.'],
  ['fa-gears', 'Workflow Engine Development', 'Custom-built workflow engines that route tasks, trigger approvals, send notifications, and escalate exceptions automatically across your teams.'],
  ['fa-diagram-project', 'System Integration & API Connectivity', 'Connect your CRM, ERP, accounting software, and other platforms so data flows seamlessly between systems without manual re-entry.'],
  ['fa-chart-bar', 'Reporting & Analytics Automation', 'Automated data collection and scheduled report generation keep stakeholders informed without anyone having to pull numbers manually.'],
  ['fa-shield-halved', 'Compliance & Audit Trail Automation', 'Build compliance checkpoints directly into your workflows and generate automatic audit logs that satisfy regulatory requirements.']
];

const coreServices = [
  ['fa-table-cells-large', 'BPA Consulting & Strategy', 'We begin with a deep-dive into your operations to identify which processes deliver the most ROI when automated — and create a roadmap to get there.'],
  ['fa-square-check', 'Custom Workflow Development', 'Our developers build automation solutions tailored to your exact processes — no off-the-shelf templates, no workarounds.'],
  ['fa-layer-group', 'Legacy System Modernization', 'We integrate automation layers over your existing systems so you capture efficiency gains without costly full replacements.'],
  ['fa-laptop-code', 'Low-Code & No-Code Deployments', 'For faster time-to-value, we deploy BPA solutions using modern low-code platforms that your team can maintain and extend over time.'],
  ['fa-shield-halved', 'Ongoing Support & Optimization', 'Business needs evolve. We monitor, refine, and expand your automation systems to keep performance high as your processes change.']
];

const contactTestimonials = [
  { quote: "The Demski Group is great to work with! They inherited a mess with our platform and have been able to fix the bugs and make the platform more user friendly. They are also very professional and timely as well as a pleasure to work with! We have worked with Andrew Demski for all of our IT needs.", name: "Delaine Mast", role: "CEO", link: "/our-clients/impact-team-tennis/" },
  { quote: "We spoke to a few different companies as we set out to find someone to create a website/database application to augment our desktop software. We chose The Demski Group in part because we found them very easy to talk to in our interview. That first impression stuck as we have a great relationship with them now.", name: "Mickey Rowe", role: "Manager", link: "/our-clients/biopac/" },
  { quote: "Code quality was very good and done in a fast, efficient manner. We are pleased with the excellent job from The Demski Group. The team was able to provide high-quality services throughout the project. They produced the deliverables in a timely manner and within budget.", name: "Thariq Kara", role: "CEO", link: "/our-clients/nu-borders/" }
];

const cases = [
  { tag: 'Biotechnology', name: 'BIOPAC Systems, Inc.', description: 'The Demski Group successfully delivered a scalable and efficient web application for Biopac, enabling users to perform and track scientific studies online.', image: 'Biopac-white.png', tone: 'dark', link: '/our-clients/biopac' },
  { tag: 'Sports', name: 'Impact Team Tennis', description: '', image: 'ImpactTennis-e1744403805766.png', tone: 'green', link: '/our-clients/impact-team-tennis/' },
  { tag: 'Health & Wellness', name: 'Prefusion Health', description: '', image: 'PrefusionHealth.png', tone: 'purple', link: '/our-clients/prefusion-health/' },
  { tag: 'Insurance', name: 'Adaptrack', description: '', image: 'ADAPTRACK.png', tone: 'rose', link: '/our-clients/' },
  { tag: 'Retail', name: 'Zoomifi', description: '', image: 'Zoomifi-2-e1744403389524.png', tone: 'pink', link: '/our-clients/zoomifi/' },
  { tag: 'Real Estate', name: 'Property Gauge', description: '', image: 'PropertyGauge.png', tone: 'sky', link: '/our-clients/property-gauge/' },
  { tag: 'Energy & Utilities', name: 'Corning', description: '', image: 'Corning-e1744401647484.png', tone: 'yellow', link: '/our-clients/corning-glass/' },
  { tag: 'Logistics', name: 'Bite', description: '', image: 'Bite-e1746208787498.png', tone: 'violet', link: '/our-clients/bite/' },
  { tag: 'Manufacturing', name: 'Saputo', description: '', image: 'Saputo.png', tone: 'sand', link: '/our-clients/' }
];
---

<Layout
  title="Business Process Automation Solutions | Streamline Operations"
  description="Automate your core business processes with custom BPA solutions. The Demski Group helps SMBs eliminate manual work, reduce costs, and scale operations efficiently."
  pageClass="bpa-page-class"
>
  <link rel="preload" href="https://demskigrdev.wpenginepowered.com/wp-content/uploads/2025/07/team-discussing-automotive-design-project-in-moder-2025-10-19-03-58-54-utc-scaled.jpg" as="image" slot="head" />

  <style slot="head">
    /* ── Page tokens ── */
    .bpa-page-class {
      --blue: #005cb9;
      --orange: #f09300;
      --ink: #282828;
      --muted: #f0f0f0;
      --navy: #001629;
      --max: 1850px;
      font-family: 'Outfit', sans-serif;
      color: var(--ink);
    }

    /* ── Nav overrides ── */
    .bpa-page-class .top-bar { background: var(--blue); min-height: 34px; padding: 6px 20px; font-size: 16px; line-height: 1; font-weight: 500; }
    .bpa-page-class header { box-shadow: none; padding: 13px 176px 11px 50px; align-items: center; }
    .bpa-page-class .logo img { height: 42px; }
    .bpa-page-class nav { gap: 28px; }
    .bpa-page-class nav a, .bpa-page-class .nav-trigger { font-size: 15px; font-weight: 500; }
    .bpa-page-class .nav-dropdown-menu { width: 320px; top: calc(100% + 14px); box-shadow: 0 4px 18px rgba(0,0,0,0.18); }
    .bpa-page-class .nav-dropdown-menu a { padding: 12px 16px; font-size: 14px; font-weight: 400; }
    .bpa-page-class .btn-cta, .bpa-page-class .footer-cta-btn { background: var(--orange) !important; border-color: var(--orange); border-radius: 6px; font-size: 15px !important; padding: 11px 18px !important; }

    /* ── Shell ── */
    .bpa-page { overflow-x: clip; }
    .bpa-section { max-width: var(--max); margin: 0 auto; padding: 60px 50px; }

    /* ── Eyebrow ── */
    .eyebrow { display: inline-flex; align-items: center; gap: 10px; color: var(--blue); font-size: 14px; line-height: 1.2; font-weight: 500; margin: 0 0 20px; }
    .eyebrow::before { content: ''; width: 11px; height: 11px; border: 1.5px solid currentColor; border-radius: 3px; transform: rotate(45deg); flex-shrink: 0; }

    /* ── Hero ── */
    .hero {
      position: relative;
      max-width: 100%;
      min-height: 540px;
      margin-left: 50px;
      margin-right: 50px;
      padding-top: 112px;
      padding-bottom: 80px;
      padding-left: 113px;
      padding-right: 80px;
      background:
        linear-gradient(to right, rgba(0,0,0,0.72) 0%, rgba(0,0,0,0.22) 100%),
        url('https://demskigrdev.wpenginepowered.com/wp-content/uploads/2025/07/team-discussing-automotive-design-project-in-moder-2025-10-19-03-58-54-utc-scaled.jpg') center 39% / cover no-repeat;
      color: #fff;
      display: flex;
      align-items: flex-start;
      justify-content: flex-start;
      border-radius: 24px;
      overflow: hidden;
    }
    .hero-content { flex: 0 1 610px; }
    .hero .eyebrow { color: rgba(255,255,255,0.92); font-size: 15px; margin-bottom: 20px; }
    .hero .eyebrow::before { border-color: rgba(255,255,255,0.70); }
    .hero h1 { max-width: 610px; margin: 0 0 22px; font-size: clamp(40px, 4.2vw, 60px); font-weight: 700; line-height: 1.18; letter-spacing: -0.5px; color: #fff; }
    .hero p { max-width: 500px; margin: 0 0 30px; font-size: 16px; font-weight: 400; line-height: 1.6; color: rgba(255,255,255,0.88); }
    .hero-btn { display: inline-flex; align-items: center; gap: 12px; background: #f0f0f0; color: #1a1a1a; padding: 14px 26px; border-radius: 10px; font-size: 17px; font-weight: 500; text-decoration: none; transition: transform 0.18s ease, background 0.18s ease; white-space: nowrap; }
    .hero-btn:hover { transform: translateY(-2px); background: #e0e0e0; }
    .hero-btn i { font-size: 14px; }
    .hero-badges { position: absolute; top: 135px; right: 137px; display: flex; flex-direction: column; align-items: flex-end; gap: 66px; }
    .hero-badges img { width: 142px; height: auto; object-fit: contain; display: block; }

    /* ── Intro grid ── */
    .intro-grid { display: grid; grid-template-columns: 0.85fr 1.15fr; gap: 100px; align-items: center; min-height: 340px; }
    .section-title { margin: 0; color: #1b1c1d; font-size: clamp(26px, 2.6vw, 38px); font-weight: 600; line-height: 1.26; letter-spacing: 0; }
    .intro-grid .section-title { max-width: 440px; font-size: clamp(33px, 3.85vw, 55px); margin-left: 5%; }
    .copy { color: #282828; font-size: 16px; line-height: 1.65; }
    .copy p { margin: 0 0 18px; }
    .copy ul { margin: 10px 0 18px 20px; padding: 0; }
    .copy li { margin-bottom: 8px; color: #282828; font-size: 16px; line-height: 1.65; list-style: disc; }

    /* ── Accordion + image ── */
    .media-accordion { display: grid; grid-template-columns: 31% 1fr; gap: 60px; align-items: stretch; padding-top: 60px; padding-bottom: 60px; }
    .media-accordion .feature-image { order: -1; }
    .feature-image { width: 100%; height: 680px; min-height: unset; max-height: unset; border-radius: 25px; object-fit: cover; object-position: center; display: block; }
    .accordion-copy { padding-top: 4px; }
    .accordion-copy .section-title { max-width: 900px; margin-bottom: 35px; font-size: clamp(28px, 3.2vw, 46px); }

    /* ── Accordion items ── */
    .acc-list { width: 100%; }
    .acc-item { border-bottom: 1px solid rgba(2,20,40,0.35); }
    .acc-btn { width: 100%; display: grid; grid-template-columns: 56px 1fr 22px; gap: 16px; align-items: center; min-height: 62px; padding: 6px 0; cursor: pointer; background: transparent; border: none; text-align: left; font-family: inherit; box-sizing: border-box; -webkit-appearance: none; appearance: none; }
    .acc-icon-box { width: 56px !important; min-width: 56px !important; height: 46px !important; border-radius: 10px; display: flex !important; align-items: center; justify-content: center; background-color: #005cb9 !important; color: #ffffff !important; font-size: 18px; box-sizing: border-box; flex-shrink: 0; }
    .acc-icon-box i { color: #ffffff !important; font-size: 18px !important; display: block; }
    .acc-title { font-size: 20px; font-weight: 500; line-height: 1.3; color: #1b1c1d; }
    .acc-toggle { justify-self: end; width: 22px; height: 22px; border: 1.5px solid #0f1720; border-radius: 3px; display: flex; align-items: center; justify-content: center; font-size: 16px; font-weight: 300; color: #0f1720; flex-shrink: 0; transition: background 0.2s, color 0.2s, border-color 0.2s; line-height: 1; }
    .acc-toggle::before { content: '+'; }
    .acc-item.active .acc-toggle { background: #005cb9; border-color: #005cb9; color: #fff; }
    .acc-item.active .acc-toggle::before { content: '−'; }
    .acc-panel { max-height: 0; overflow: hidden; transition: max-height 0.28s ease; }
    .acc-item.active .acc-panel { max-height: 400px; }
    .acc-body { max-width: 900px; padding: 12px 0 20px 78px; font-size: 16px; line-height: 1.65; color: #333; }

    /* ── Services grid ── */
    .services-grid-section { display: grid; grid-template-columns: 31% 1fr; gap: 50px; padding-top: 50px; padding-bottom: 50px; }
    .services-grid-section .section-title { max-width: 300px; font-size: 40px; color: #1b1c1d; font-family: 'Outfit', sans-serif; }
    .service-grid { display: grid; grid-template-columns: repeat(2, minmax(0,1fr)); gap: 40px 70px; padding-left: 100px; }
    .service-card i { display: block; margin-bottom: 14px; color: #005cb9; font-size: 28px; line-height: 1; }
    .service-card h3 { margin: 0 0 10px; font-size: 25px; font-weight: 500; line-height: 1.3; color: #282828; font-family: 'Outfit', sans-serif; }
    .service-card p { margin: 0; color: #000; font-size: 16px; line-height: 1.55; }

    /* ── Why Choose ── */
    .why-section-wrap { position: relative; padding: 0; margin: 50px 100px; }
    .why-section { display: block; border-radius: 22px; overflow: hidden; background-color: #f0f0f0; }
    .why-copy { flex: 1; min-width: 0; padding: 56px 56px 52px; }
    .why-copy .eyebrow { color: #005cb9; margin-bottom: 16px; }
    .why-copy .section-title { margin-bottom: 26px; font-size: clamp(33px, 3.85vw, 55px); font-weight: 600; }
    .why-copy > p { color: #111; font-size: 16px; line-height: 1.7; margin-bottom: 10px; max-width: 830px; }
    .testimonial { margin-top: 30px; padding-left: 18px; border-left: 3px solid #001629; }
    .testimonial h3 { margin: 0 0 18px; font-size: clamp(22px, 2.2vw, 33px); font-weight: 600; line-height: 1.25; color: #1b1c1d; }
    .quote-mark { color: #005cb9; font-size: 26px; line-height: 1; margin-bottom: 14px; letter-spacing: -3px; }
    .testimonial blockquote { margin: 0 0 30px; max-width: 1040px; color: #282828; font-size: 25px; font-weight: 400; font-family: 'Outfit', sans-serif; line-height: 1.65; }
    .testimonial-footer { display: flex; align-items: flex-end; justify-content: space-between; gap: 16px; padding-top: 4px; }
    .testimonial-footer p { margin: 0; font-size: 16px; line-height: 1.55; color: #333; }
    .testi-fraction { text-align: center; font-size: 16px; color: #888; margin-top: 22px; }
    .text-link { color: #282828; text-decoration: none; font-weight: 600; white-space: nowrap; font-size: 16px; }

    /* ── Why card ── */
    .why-card { position: absolute; top: -14px; right: 0; bottom: -14px; width: 355px; margin: 14px 0 14px 14px; background-color: #001629 !important; color: #ffffff; display: flex; flex-direction: column; border-radius: 20px; z-index: 2; }
    .why-card-img-frame { padding: 24px 22px 16px; flex-shrink: 0; }
    .why-card-img-frame img { width: 100%; height: 420px; object-fit: cover; object-position: center top; border-radius: 16px; display: block; }
    .why-card-body { padding: 22px 26px 42px; display: flex; flex-direction: column; align-items: center; flex: 1; }
    .why-card h3 { width: 100%; margin: 0 0 44px; color: #ffffff !important; font-size: 25px; font-weight: 600; line-height: 1.35; text-align: left; font-family: 'Outfit', sans-serif; }
    .button-light { display: flex; align-items: center; justify-content: center; gap: 10px; width: 90%; min-height: 41px; background-color: #ffffff; color: #021428; padding: 12px 16px; font-size: 19px; font-weight: 600; font-family: 'Outfit', sans-serif; border-radius: 10px; text-decoration: none; text-align: center; white-space: normal; flex-wrap: wrap; transition: transform 0.2s ease; }
    .button-light:hover { transform: translateY(-3px); }

    /* ── Case studies ── */
    .case-section { padding-top: 60px; padding-bottom: 50px; }
    .case-section .section-title { max-width: 600px; margin-bottom: 14px; font-size: clamp(24px, 2.8vw, 40px); }
    .case-intro { max-width: 660px; margin: 0 0 36px; color: #000; font-size: 14px; line-height: 1.55; }
    .case-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 16px; }
    .case-card { position: relative; min-height: 148px; padding: 32px 22px 18px; border-radius: 16px; background: #fff; box-shadow: 0 2px 14px rgba(0,0,0,0.14); overflow: hidden; display: flex; flex-direction: column; justify-content: center; transition: box-shadow 0.25s ease; }
    .case-card:hover { box-shadow: 0 9px 44px rgba(0,0,0,0.24); }
    .case-card.featured { grid-column: span 2; grid-row: span 2; min-height: 310px; background: #282828; color: #fff; justify-content: flex-start; }
    .case-tag { position: absolute; top: 0; left: 0; width: 136px; padding: 5px 10px; border-radius: 0 0 6px 0; background: #8ce9ac; color: #000; font-size: 12px; font-weight: 700; text-align: center; }
    .case-card.featured .case-tag { width: 270px; background: #001a34; color: #fff; }
    .case-card.purple .case-tag { background: #686de4; color: #fff; }
    .case-card.rose .case-tag { background: #f2969d; }
    .case-card.pink .case-tag { background: #e585d9; }
    .case-card.sky .case-tag { background: #91c8f3; }
    .case-card.yellow .case-tag { background: #f0e58c; }
    .case-card.violet .case-tag { background: #c47aeb; }
    .case-card.sand .case-tag { background: #f1c88b; }
    .case-card img { align-self: center; max-width: 75%; max-height: 92px; object-fit: contain; margin-top: 29px; }
    .case-card.featured img { align-self: flex-start; max-width: 322px; max-height: 115px; margin: 44px 0 16px; }
    .case-card p { margin: 0 0 8px; color: #fff; font-size: 14px; line-height: 1.5; }
    .case-card .case-link { align-self: flex-end; margin-top: auto; color: inherit; text-decoration: none; font-weight: 600; white-space: nowrap; font-size: 13px; display: inline-flex; align-items: center; gap: 4px; }
    .case-card:not(.featured) .case-link { color: #005cb9; }

    /* ── Contact ── */
    .contact-section { background-color: #001629; margin-top: 50px; }
    .contact-inner { display: grid; grid-template-columns: 62% 38%; max-width: var(--max); margin: 0 auto; min-height: 600px; align-items: stretch; }
    .contact-form-col { padding: 72px 64px; }
    .contact-form-col .section-title { color: #fff; font-size: clamp(28px, 3.2vw, 46px); margin-bottom: 44px; line-height: 1.2; font-weight: 600; }
    .contact-form { display: flex; flex-direction: column; gap: 28px; }
    .form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 40px; }
    .contact-form input, .contact-form textarea { width: 100%; padding: 10px 0; border: none; border-bottom: 1px solid rgba(255,255,255,0.3); border-radius: 0; font-size: 14px; font-family: inherit; outline: none; background: transparent; color: #ffffff; transition: border-bottom-color 0.2s; }
    .contact-form input:focus, .contact-form textarea:focus { border-bottom-color: rgba(255,255,255,0.75); }
    .contact-form input::placeholder, .contact-form textarea::placeholder { color: rgba(255,255,255,0.45); }
    .contact-form textarea { resize: none; min-height: 80px; }
    .form-submit { align-self: flex-start; padding: 15px 44px; background: #ffffff; color: #001629; border: none; border-radius: 8px; font-size: 15px; font-weight: 600; font-family: inherit; cursor: pointer; margin-top: 12px; transition: background 0.2s; }
    .form-submit:hover { background: #f0f0f0; }
    .contact-right-col { background-color: #f0f0f0; border-radius: 25px; margin: 28px 28px 28px 0; padding: 40px 40px 0; display: flex; flex-direction: column; box-shadow: 0 4px 32px rgba(0,0,0,0.14); }
    .contact-right-label { font-size: 16px; font-weight: 600; color: #1a1a1a; margin: 0 0 22px; }

    /* ── Testimonial carousel ── */
    .tc-carousel { position: relative; flex: 1; }
    .tc-slide { display: none; flex-direction: column; }
    .tc-slide.active { display: flex; }
    .tc-quote-icon { color: #005cb9; font-size: 26px; margin-bottom: 18px; line-height: 1; }
    .tc-text { color: #333; font-size: 16px; line-height: 1.72; margin: 0 0 20px; flex: 1; }
    .tc-footer { display: flex; justify-content: space-between; align-items: flex-end; gap: 12px; padding-top: 14px; border-top: 1px solid #e8e8e8; }
    .tc-author { font-size: 14px; font-weight: 700; color: #1a1a1a; line-height: 1.4; }
    .tc-author span { display: block; font-weight: 400; font-size: 13px; color: #888; }
    .tc-link { color: #1a1a1a; text-decoration: none; font-size: 13px; font-weight: 500; white-space: nowrap; display: inline-flex; align-items: center; gap: 4px; }
    .tc-fraction-bar { text-align: center; font-size: 18px; font-weight: 600; color: #1a1a1a; padding: 8px 0 12px; margin-top: auto; margin-bottom: 28px; }
    .contact-right-img { width: 100%; height: 220px; object-fit: cover; object-position: center top; display: block; border-radius: 16px 16px 22px 22px; }

    .bpa-page-class footer { margin-top: 0; position: relative; z-index: 1; }

    /* ── Responsive ≤1200px ── */
    @media (max-width: 1200px) {
      .bpa-page-class header { padding: 14px 28px; }
      .intro-grid, .services-grid-section, .contact-inner { grid-template-columns: 1fr; }
      .media-accordion { grid-template-columns: 1fr; }
      .media-accordion .feature-image { display: none; }
      .service-grid { padding-left: 0; gap: 32px 40px; }
      .case-grid { grid-template-columns: 1fr 1fr; }
      .contact-form-col { padding: 44px 36px; }
      .contact-right-col { margin: 22px 22px 0 0; padding: 32px 32px 0; border-radius: 22px; }
      .why-section-wrap { padding: 0 20px; }
      .why-copy { padding: 42px 36px 38px; }
    }

    /* ── Responsive ≤1100px ── */
    @media (max-width: 1100px) {
      .hero { flex-direction: column; align-items: flex-start; gap: 28px; }
      .hero-content { flex: none; max-width: 640px; }
      .hero p { margin-left: 0; }
      .hero-btn { margin-left: 0; }
      .hero-badges { position: static; flex-direction: row; align-items: center; gap: 12px; margin-top: 28px; }
      .hero-badges img { width: 90px; }
    }

    /* ── Responsive ≤768px ── */
    @media (max-width: 768px) {
      .bpa-page-class .top-bar { font-size: 12px; }
      .bpa-page-class .logo img { height: 36px; }
      .bpa-section { padding: 40px 20px; }
      .hero { min-height: auto; margin-left: 16px; margin-right: 16px; padding: 44px 32px; gap: 20px; border-radius: 16px; }
      .hero h1 { font-size: clamp(30px, 8vw, 38px); max-width: 100%; }
      .hero p { font-size: 16px; max-width: 100%; margin-left: 0; }
      .hero-btn { margin-left: 0; font-size: 16px; }
      .hero-badges { position: static; flex-wrap: wrap; gap: 12px; margin-top: 24px; }
      .hero-badges img { width: 110px; }
      .acc-btn { grid-template-columns: 48px 1fr 20px; gap: 12px; }
      .acc-icon-box { width: 48px !important; min-width: 48px !important; height: 38px !important; font-size: 15px; }
      .acc-title { font-size: 14px; }
      .acc-body { padding-left: 60px; font-size: 13px; }
      .service-grid, .case-grid { grid-template-columns: 1fr; }
      .case-card.featured { grid-column: span 1; }
      .why-copy { padding: 28px 20px; }
      .why-section-wrap { padding: 0 12px; margin: 30px 0; }
      .why-card-img-frame img { height: 280px; }
      .why-card-body { padding: 18px 14px 24px; }
      .testimonial blockquote { font-size: 15px; }
      .contact-form-col { padding: 36px 22px; }
      .contact-right-col { margin: 20px 0 0 0; padding: 28px 22px 0; border-radius: 22px; }
      .form-row { grid-template-columns: 1fr; }
      .contact-right-img { height: 180px; }
    }

    /* ── Responsive ≤600px ── */
    @media (max-width: 600px) {
      .bpa-section { padding: 32px 16px; }
      .hero { margin-left: 12px; margin-right: 12px; padding: 36px 20px; border-radius: 14px; }
      .hero h1 { font-size: clamp(28px, 7.5vw, 36px); }
      .hero p { font-size: 15px; margin-left: 0; }
      .hero-btn { margin-left: 0; font-size: 15px; }
      .intro-grid { gap: 24px; }
      .intro-grid .section-title { font-size: clamp(24px, 7vw, 32px); margin-left: 0; }
      .accordion-copy .section-title { font-size: clamp(20px, 6vw, 28px); }
      .acc-btn { grid-template-columns: 42px 1fr 18px; gap: 10px; min-height: 54px; }
      .acc-icon-box { width: 42px !important; min-width: 42px !important; height: 36px !important; font-size: 14px; }
      .acc-title { font-size: 13px; }
      .acc-body { padding-left: 52px; font-size: 13px; }
      .services-grid-section { gap: 20px; padding-top: 24px; padding-bottom: 24px; }
      .services-grid-section .section-title { font-size: 28px; }
      .service-grid { grid-template-columns: 1fr; gap: 24px; padding-left: 0; }
      .service-card h3 { font-size: 18px; }
      .service-card p { font-size: 14px; }
      .why-section-wrap { padding: 0 12px; margin: 24px 0; }
      .why-copy { padding: 28px 18px 24px !important; }
      .why-copy .section-title { font-size: clamp(22px, 6vw, 30px); }
      .why-copy > p { font-size: 14px; }
      .why-card { position: relative !important; top: auto !important; right: auto !important; bottom: auto !important; width: 100% !important; margin: 0 !important; border-radius: 0 0 14px 14px !important; }
      .why-card-img-frame img { height: 200px; }
      .why-card-body { padding: 16px 16px 24px; }
      .why-card h3 { font-size: 18px; margin-bottom: 20px; }
      .button-light { font-size: 16px; width: auto; padding: 13px 28px; min-height: auto; }
      .testimonial blockquote { font-size: 14px; }
      .testimonial h3 { font-size: 18px; }
      .case-grid { grid-template-columns: 1fr; gap: 12px; }
      .case-card.featured { grid-column: span 1; grid-row: span 1; min-height: 200px; }
      .case-card { min-height: 120px; padding: 28px 16px 14px; }
      .contact-section { margin-top: 30px; }
      .contact-inner { grid-template-columns: 1fr; min-height: auto; }
      .contact-form-col { padding: 32px 18px; }
      .contact-form-col .section-title { font-size: clamp(20px, 6vw, 28px); margin-bottom: 24px; }
      .form-row { grid-template-columns: 1fr; gap: 20px; }
      .form-submit { width: 100%; text-align: center; padding: 14px 20px; }
      .contact-right-col { margin: 0; padding: 24px 18px 0; border-radius: 0; }
      .contact-right-img { height: 160px; }
    }

    @media (prefers-reduced-motion: reduce) {
      .hero-btn, .button-light { transition: none; }
      .acc-panel { transition: none !important; }
    }

    @media (max-width: 430px) {
      .bpa-section { padding: 28px 16px; }
      .hero { padding: 32px 18px; margin-left: 10px; margin-right: 10px; }
      .hero h1 { font-size: clamp(26px, 7vw, 32px); }
      .hero p { font-size: 14px; }
      .hero-btn { font-size: 14px; padding: 12px 20px; }
      .hero-badges img { width: 90px; }
      .intro-grid .section-title { font-size: clamp(22px, 6vw, 28px); }
      .accordion-copy .section-title { font-size: clamp(20px, 5.5vw, 26px); }
      .acc-title { font-size: 13px; }
      .services-grid-section .section-title { font-size: clamp(22px, 6vw, 28px); }
      .service-card h3 { font-size: 17px; }
      .why-copy .section-title { font-size: clamp(20px, 5.5vw, 26px); }
      .contact-form-col .section-title { font-size: clamp(20px, 5.5vw, 26px); }
    }
  </style>

  <main class="bpa-page">

    <!-- ══ HERO ══ -->
    <section class="bpa-section hero">
      <div class="hero-content">
        <div class="eyebrow">Solutions</div>
        <h1>Streamline Every Process. Scale Every Operation.</h1>
        <p>Business Process Automation eliminates the manual, repetitive work slowing your team down — replacing it with intelligent, reliable systems that run faster, cost less, and scale with your business.</p>
        <a class="hero-btn" href="/contact-us/">
          <i class="fa-solid fa-square-arrow-up-right"></i> Schedule a Consultation
        </a>
      </div>
      <div class="hero-badges" aria-label="Review badges">
        <img src="https://demskigrdev.wpenginepowered.com/wp-content/uploads/2025/06/Clutch2024.png" alt="Clutch 2024 Top Software Developers badge" width="148" height="148" loading="eager" />
        <img src="https://demskigrdev.wpenginepowered.com/wp-content/uploads/2025/06/Clutch2023.png" alt="Clutch 2023 Top Software Developers badge" width="148" height="148" loading="lazy" />
        <img src="https://demskigrdev.wpenginepowered.com/wp-content/uploads/2025/06/Google.png" alt="Google five star review badge" width="148" height="148" loading="lazy" />
      </div>
    </section>

    <!-- ══ INTRO GRID ══ -->
    <section class="bpa-section intro-grid">
      <h2 class="section-title">What Is Business Process Automation?</h2>
      <div class="copy">
        <p>Business Process Automation (BPA) uses software, rules-based logic, and AI to execute routine tasks automatically — without human intervention. Unlike one-off tools, BPA connects your entire operation into a coordinated, self-running system.</p>
        <p>The Demski Group designs and builds BPA solutions that integrate with your existing systems and adapt to your specific workflows. Whether you need to automate onboarding, invoicing, approvals, or reporting, we build it to fit:</p>
        <ul>
          <li>Automate multi-step workflows across departments</li>
          <li>Reduce manual data entry and processing errors</li>
          <li>Trigger actions automatically based on rules or events</li>
          <li>Connect siloed systems so data flows without re-entry</li>
        </ul>
        <p>The result is a leaner, faster operation — with your team focused on work that actually requires human judgment.</p>
      </div>
    </section>

    <!-- ══ STRATEGIC BENEFITS ACCORDION ══ -->
    <section class="bpa-section media-accordion">
      <div class="accordion-copy">
        <div class="eyebrow">Service Benefits</div>
        <h2 class="section-title">Why Automate Your Business Processes?</h2>
        <div class="acc-list" data-acc>
          {strategicBenefits.map(([icon, title, text], i) => (
            <div class={`acc-item${i === 0 ? ' active' : ''}`}>
              <button class="acc-btn" type="button" aria-expanded={i === 0 ? 'true' : 'false'}>
                <span class="acc-icon-box"><i class={`fa-solid ${icon}`} aria-hidden="true"></i></span>
                <span class="acc-title">{title}</span>
                <span class="acc-toggle" aria-hidden="true"></span>
              </button>
              <div class="acc-panel" style={i === 0 ? 'max-height:500px' : ''}>
                <div class="acc-body">{text}</div>
              </div>
            </div>
          ))}
        </div>
      </div>
      <img
        class="feature-image"
        src="https://demskigrdev.wpenginepowered.com/wp-content/uploads/2025/04/engineer-working-on-chip-development-2025-03-06-13-42-24-utc-1024x683.jpg"
        alt="Engineer working on process automation"
        width="1024" height="683"
        loading="lazy"
      />
    </section>

    <!-- ══ KEY FEATURES GRID ══ -->
    <section class="bpa-section services-grid-section">
      <h2 class="section-title">What We Automate</h2>
      <div class="service-grid">
        {services.map(([icon, title, text]) => (
          <article class="service-card">
            <i class={`fa-solid ${icon}`} aria-hidden="true"></i>
            <h3>{title}</h3>
            <p>{text}</p>
          </article>
        ))}
      </div>
    </section>

    <!-- ══ CORE SERVICES ACCORDION ══ -->
    <section class="bpa-section media-accordion">
      <div class="accordion-copy">
        <div class="eyebrow">Our Process</div>
        <h2 class="section-title">Our BPA Services</h2>
        <div class="acc-list" data-acc>
          {coreServices.map(([icon, title, text], i) => (
            <div class={`acc-item${i === 0 ? ' active' : ''}`}>
              <button class="acc-btn" type="button" aria-expanded={i === 0 ? 'true' : 'false'}>
                <span class="acc-icon-box"><i class={`fa-solid ${icon}`} aria-hidden="true"></i></span>
                <span class="acc-title">{title}</span>
                <span class="acc-toggle" aria-hidden="true"></span>
              </button>
              <div class="acc-panel" style={i === 0 ? 'max-height:500px' : ''}>
                <div class="acc-body">{text}</div>
              </div>
            </div>
          ))}
        </div>
      </div>
      <img
        class="feature-image"
        src="https://demskigrdev.wpenginepowered.com/wp-content/uploads/2025/07/bearded-businessman-pointing-at-infographics-near-2024-11-13-09-25-48-utc-1024x683.jpg"
        alt="Businessman reviewing process automation results"
        width="1024" height="683"
        loading="lazy"
      />
    </section>

    <!-- ══ WHY CHOOSE ══ -->
    <div class="why-section-wrap">
      <section class="why-section">
        <div class="why-copy" style="padding-right: 410px;">
          <div class="eyebrow">Choose Us</div>
          <h2 class="section-title">Why Choose the Demski Group?</h2>
          <p>We've spent over a decade helping small and mid-sized businesses cut operational waste and build systems that actually work. We don't sell you a platform — we build the right solution for your specific processes.</p>
          <p>Our BPA engagements are always grounded in your real workflows — not generic templates. We design around how your business actually operates, then automate it intelligently.</p>
          <p><strong>What sets us apart:</strong></p>
          <ul style="margin: 14px 0 24px 22px; padding: 0;">
            <li style="color: #111; font-size: 16px; line-height: 1.7; margin-bottom: 7px; list-style: disc;">Process-first approach — we map before we build</li>
            <li style="color: #111; font-size: 16px; line-height: 1.7; margin-bottom: 7px; list-style: disc;">Vendor-neutral — we recommend what fits, not what we resell</li>
            <li style="color: #111; font-size: 16px; line-height: 1.7; margin-bottom: 7px; list-style: disc;">Integration-ready — works with your existing tools and systems</li>
            <li style="color: #111; font-size: 16px; line-height: 1.7; margin-bottom: 7px; list-style: disc;">Long-term partnership — we optimize as your business evolves</li>
          </ul>
          <div class="testimonial">
            <h3>Client Success Stories</h3>
            <div class="quote-mark">&ldquo;&ldquo;</div>
            <blockquote>The Demski Group is great to work with! They inherited a mess with our platform and have been able to fix the bugs and make the platform more user friendly. They are also very professional and timely as well as a pleasure to work with! We have worked with Andrew Demski for all of our IT needs.</blockquote>
            <div class="testimonial-footer">
              <p>Delaine Mast<br />CEO</p>
              <a class="text-link" href="/our-clients/impact-team-tennis/">Learn more &rarr;</a>
            </div>
            <div class="testi-fraction">1 / 1</div>
          </div>
        </div>
      </section>
      <aside class="why-card">
        <div class="why-card-img-frame">
          <img
            src="https://demskigrdev.wpenginepowered.com/wp-content/uploads/2025/05/colleagues-in-office-using-computer-2024-10-18-17-41-53-utc-1024x683.jpg"
            alt="Team working together on automation"
            width="1024" height="683"
            loading="lazy"
          />
        </div>
        <div class="why-card-body">
          <h3>Ready to Automate<br/>Your Operations?</h3>
          <a class="button-light" href="/contact-us/">
            Contact Us <i class="fa-solid fa-up-right-from-square"></i>
          </a>
        </div>
      </aside>
    </div>

    <!-- ══ CASE STUDIES ══ -->
    <section class="bpa-section case-section">
      <div class="eyebrow">Case Studies</div>
      <h2 class="section-title">Over 600 Projects Delivered Successfully</h2>
      <p class="case-intro">With over 12 years of software development experience in many industries, we have a lot to talk about. Here are just a few of our success stories.</p>
      <div class="case-grid">
        {cases.map(({ tag, name, description, image, tone, link }) => (
          <article class={`case-card ${tone === 'dark' ? 'featured' : tone}`}>
            <span class="case-tag">{tag}</span>
            <img src={`${assetBase}/${image}`} alt={`${name} logo`} width="300" height="92" loading="lazy" />
            {description && <p>{description}</p>}
            <a class="case-link" href={link}>Read full case study &rarr;</a>
          </article>
        ))}
      </div>
    </section>

    <!-- ══ CONTACT ══ -->
    <ContactSection />

  </main>

  <script is:inline>
    (function () {
      var page = document.querySelector('.bpa-page');
      if (!page) return;

      /* Accordions */
      page.querySelectorAll('[data-acc]').forEach(function (list) {
        var items = Array.from(list.querySelectorAll('.acc-item'));
        items.forEach(function (item) {
          var btn = item.querySelector('.acc-btn');
          if (!btn) return;
          btn.addEventListener('click', function () {
            var wasActive = item.classList.contains('active');
            items.forEach(function (i) {
              i.classList.remove('active');
              var b = i.querySelector('.acc-btn');
              if (b) b.setAttribute('aria-expanded', 'false');
              var p = i.querySelector('.acc-panel');
              if (p) p.style.maxHeight = '';
            });
            if (!wasActive) {
              item.classList.add('active');
              btn.setAttribute('aria-expanded', 'true');
              var panel = item.querySelector('.acc-panel');
              if (panel) panel.style.maxHeight = '500px';
            }
          });
        });
      });

      /* Testimonial carousel */
      var slides = Array.from(document.querySelectorAll('[data-tc-slide]'));
      var fracEl  = document.getElementById('tcFraction');
      if (!slides.length) return;
      var cur = 0;
      var carousel = document.getElementById('tcCarousel');
      function showSlide(n) {
        slides[cur].classList.remove('active');
        cur = (n + slides.length) % slides.length;
        slides[cur].classList.add('active');
        if (fracEl) fracEl.textContent = (cur + 1) + ' / ' + slides.length;
      }
      var timer = setInterval(function () { showSlide(cur + 1); }, 8000);
      if (carousel) {
        carousel.addEventListener('mouseenter', function () { clearInterval(timer); });
        carousel.addEventListener('mouseleave', function () {
          timer = setInterval(function () { showSlide(cur + 1); }, 8000);
        });
      }
    })();
  </script>
</Layout>
